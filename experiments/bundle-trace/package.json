{
  "name": "bundle-trace-experiment",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "cargo build --locked --release",
    "test": "cargo test --locked && node scripts/verify.mjs && node scripts/verify-formats.mjs",
    "collect": "node scripts/collect.mjs"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0",
    "esbuild": "0.25.12"
  }
}
