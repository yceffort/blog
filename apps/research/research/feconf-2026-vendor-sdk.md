---
title: '외부 SDK를 뜯어 다시 만들었습니다: 전송용 번들을 69% 줄이고, 검증의 빈틈 찾기'
marp: true
paginate: true
theme: kakao
transition: glide
tags:
  - feconf
  - bundler
  - tree-shaking
  - testing
  - sdk
date: 2026-09-15
post: https://yceffort.kr/2026/08/rebuilding-a-vendor-sdk
description: '상수 하나를 import했는데 gzip 91KiB가 따라왔습니다. 원인을 실험으로 가르고, 배포된 소스맵과 타입 선언에서 SDK를 복원해 전송용 번들을 69% 줄였습니다. 조건 분기와 의존성 예측에서 틀린 과정, 원본 대조와 일부러 망가뜨려 보는 검사로 확인한 동작과 남은 차이를 공유합니다.'
published: false
---

<!-- _class: lead invert -->

# 외부 SDK를 뜯어 다시 만들었습니다

## 전송용 번들을 69% 줄이고, 검증의 빈틈 찾기

<br>

FEConf 2026 · yceffort · 카카오페이증권

---

## 👋 카카오페이증권의 홈을 만드는 개발자입니다

**yceffort**, 카카오페이증권 프론트엔드 엔지니어

- 『모던 리액트 Deep Dive』, 『npm Deep Dive』, 『프런트엔드 성능 최적화 Deep Dive』 저자
- DAN 24에서는 웹서비스의 번들 최적화를 발표했습니다
  오늘은 **외부 SDK 하나의 진단과 재구성**을 다룹니다

홈은 사용자가 앱을 열 때 가장 먼저 보는 화면입니다
**첫 화면에 필요 없는 JavaScript의 다운로드·파싱·실행 비용**부터 확인했습니다

[yceffort.kr](https://yceffort.kr) · [github.com/yceffort](https://github.com/yceffort)

<!-- 공개 가능한 규모 수치는 확인되면 추가한다. 발표의 필수 근거는 아니다. -->

---

## 이야기는 이 두 줄에서 시작합니다

```ts
import {AssignReason} from '@vendor/js-sdk'

console.log(AssignReason.NO_RULE_MATCH)
```

> 실험 플랫폼 SaaS의 웹 SDK입니다
> `AssignReason` 은 **"이 유저가 왜 A군에 배정됐는지"** 를
> 알려주는 열거형이고, 문자열 상수 몇 개가 전부입니다

SDK를 초기화하지도 않았습니다
**문자열 상수 하나를 읽었을 뿐입니다**

그런데 이 파일이 이상하게 무거웠습니다

---

<!-- _class: map -->

## 이 SDK가 어떻게 생겼냐면

```text
웹 SDK
├── 평가 엔진       타겟 조건을 비교하고 유저 ID로 A/B 배정
├── 메시지 UI       배너, 팝업, 리다이렉트
└── 이벤트 전송     track: 유저 행동을 서버로 전송

별도 React 바인딩 패키지
└── 웹 SDK를 감싸는 Provider, Context, 훅
```

**홈이 쓰는 기능은 이벤트 전송입니다**
웹 SDK의 초기화 경로에는 평가와 메시지 기능까지 연결되어 있었습니다

<!-- React 바인딩이 웹 SDK 안에 번들된다는 뜻은 아니다. 지도 설명은 30초. -->

---

## 측정해봤더니

| 시나리오                     | raw       | gzip     |
| ---------------------------- | --------- | -------- |
| 상수 하나만 import           | 약 384 KB | 약 94 KB |
| 실사용 (`createClient` 호출) | 약 393 KB | 약 97 KB |

상수 하나를 가져왔을 뿐인데 **raw 기준 실사용의 97.7%** 가 그대로 남았습니다

<br>

### 문자열 상수 하나를 읽는 데 gzip 약 91KiB가 따라왔습니다

초기 분석 스냅샷 · esbuild 0.24, gzip 레벨 9

<!-- 패키지·API·서브패스 이름은 역할을 남긴 가명이다. 97.7%는 두 번들의 raw 크기 비율이며 미사용 코드 비율이 아니다. 최종 크기는 2026-09-05 갱신본의 별도 측정이다. -->

---

## 보존할 동작과 허용할 차이를 먼저 나눴습니다

- **보존:** 평가·버킷팅 로직, 대조 대상으로 정한 이벤트 필드
- **변경:** 조립, 엔트리, 폴리필, UUID·base64 유틸리티
- **의도적 차이:** 전송용 엔트리는 설정 동기화를 기다리지 않아
  시작 시점이 빠르고, 이벤트의 일부 내부 메타데이터가 비어 있습니다

**평가 엔트리는 배정을, 전송용 엔트리는 이벤트 계약을 대조합니다**
전송용의 차이가 실제 수집·집계에 미치는 영향은 별도의 검증 대상입니다

<!-- 전송용 엔트리는 internalProperties가 빈 객체다. 원본은 evaluation_mode와 config_modified_at 등 설정 메타데이터를 싣는다. 대시보드 집계 영향은 코드 대조만으로 확인되지 않는다. -->

---

<!-- _class: part invert -->

# 1. 왜 안 줄어드는가

## 원인이 둘인데, 처방이 다릅니다

---

## 후보가 둘입니다

트리셰이킹은 번들러가 "이 코드는 아무도 안 쓴다"고 판단되면 빼 주는 최적화입니다
그게 거의 작동하지 않았습니다

<br>

**1. 번들러가 확신을 못 한다 (안전성)**
배포본의 클래스가 IIFE로 컴파일되어, 호출을 지워도 안전한지 증명하기 어려웠습니다
`sideEffects`는 모듈 단위, `PURE`는 호출 단위의 부수효과 힌트입니다

**2. 애초에 진짜로 쓰인다 (도달 가능성)**
초기화 경로가 여러 기능을 참조하면, 번들러가 사용하지 않는 기능을 가려내기 어렵습니다

---

## 그래서 강제로 주입해서 각각 쟀습니다

```js
// /*#__PURE__*/ = "이 호출은 부작용이 없으니 안 쓰면 지워도 된다"
const evaluator = /*#__PURE__*/ createEvaluator()
```

배포본의 클래스 IIFE에 PURE 주석을 주입해, 부수효과 힌트의 영향을 비교했습니다
**원인 분리를 위한 실험입니다, 이 결과만으로 동작 보존까지 확인되지는 않습니다**

| 시나리오                           | gzip     | 변화       |
| ---------------------------------- | -------- | ---------- |
| 상수 하나만 import (원본)          | 약 94 KB |            |
| 상수 하나만 import + **PURE 주입** | 약 25 KB | **-73.6%** |
| 실사용 (`createClient` 호출, 원본) | 약 97 KB |            |
| 실사용 + **PURE 주입**             | 약 96 KB | **-1.2%**  |

<!-- 초기 분석 스냅샷의 실험. 최종 갱신본의 재실험 결과로 제시하지 않는다. -->

---

<!-- _class: big -->

## 읽는 법

<br>

**상수만 쓸 때는 73.6% 줄어듭니다**
부수효과 힌트가 제거 여부에 큰 영향을 줬습니다

**그런데 실제로 SDK를 쓰기 시작하면 1.2%밖에 안 줍니다**
실사용 초기화 경로에서는 많은 기능의 참조가 남아, 같은 힌트만으로는 줄지 않았습니다

<br>

**실사용 번들을 줄이려면 초기화 구조와 엔트리까지 바꿔야 했습니다**
PURE 주입의 효과를 `sideEffects` 설정 한 줄의 효과로 해석하지는 않습니다

---

<!-- _class: you -->

## 앱에서는 같은 SDK 청크를 받았습니다

Next.js 16 + Turbopack의 **한 빌드 안에** 라우트 셋을 두었습니다
이 실험은 웹 SDK를 재export하는 **React 바인딩 패키지**를 import합니다

```bash
$ grep -o 'static/chunks/[a-z0-9_-]*\.js' .next/server/app/min.html | sort -u
static/chunks/0k_6yl401mp01.js   # 페이지 고유
static/chunks/2izqmkhuyaa9a.js   # min과 init이 공유하는 SDK 청크
```

| 라우트      | 하는 일                       | SDK 청크 raw |
| ----------- | ----------------------------- | ------------ |
| `/baseline` | SDK 없음                      | 없음         |
| `/min`      | 상수 하나 import              | 약 409 KB    |
| `/init`     | `createClient` + `track` 호출 | 같은 청크    |

**상수만 쓰는 페이지도 이 SDK 청크를 로드했습니다**
제거 가능성은 같은 진입점의 최소 import와 초기화를 **별도 빌드**해서 비교해야 합니다

<!--
2026-09-15 실측. ~/works/hackle-treeshake (공개 저장소, 벤더 실명이 들어 있어 슬라이드에는 URL을 넣지 않음).
Next.js 16.3.3 Turbopack, Node 24.20. 공유 청크 6개 제외. /min과 /init은 SDK 청크(408,870 B)를 공유하고 페이지 고유 청크(307 B, 379 B)만 다르다.
gzip은 node zlib level 9 기준. gzip -9 CLI로는 SDK 청크가 96,945 B.
슬라이드의 라우트 이름은 저장소의 /hackle-min, /hackle-init을 익명화한 것.
-->

---

<!-- _class: you -->

## 그 청크를 `source-map-explorer`로 열면

![w:760](/feconf-2026/source-map-explorer.png)

**상수만 쓰는 페이지도 이 공유 청크를 받습니다**
영역을 제거할지는 전송 계약과 실행 환경을 확인한 뒤 결정합니다

<!--
productionBrowserSourceMaps: true 로 빌드한 뒤 source-map-explorer 2.5.3 으로 SDK 청크를 연 화면.
Turbopack 청크는 sourceMappingURL이 다른 이름의 맵을 가리켜 맵 경로를 명시하고 --no-border-checks 가 필요했다. 맵 못 찾은 바이트 0.03%.
캡처는 맵의 sources 경로에서 벤더명을 vendor 로 치환하고, 최상위 디렉터리(node_modules, @vendor, js-sdk, src, vendor, core, internal, core-js) 외의 파일/디렉터리 이름은 █로 가린 것. 색 오버레이는 iam, webapp, property, core-js, evaluation, model 디렉터리의 실제 상자 위치에 얹은 것. 상자 크기와 구조는 실측 그대로.
-->

---

## 2. 왜 재구성까지 해 보았나

**필요한 기능은 전송인데, 초기화하면 다른 기능도 따라왔습니다**

| 선택          | 이번 문제에서 확인한 것                         |
| ------------- | ----------------------------------------------- |
| PURE 힌트     | 초기화 번들은 1.2%만 감소                       |
| 기능별 엔트리 | 전송 경로에서 평가·메시지 조립을 분리할 수 있음 |
| 소스 복원     | 원본 로직을 확보해 재구성 전후를 대조할 수 있음 |

이 근거로 **재구성 가능성을 검증했습니다**
지연 로딩의 효과, 벤더 개선 일정, 자체 유지 비용까지 비교한 결과는 아닙니다

<!-- 벤더 요청 경위·지연 로딩 비교·작업 시간은 확보한 자료에 없다. 실제로 검토하거나 배제했다고 꾸며 쓰지 않는다. 유지 책임은 결론의 별도 장에서 다룬다. -->

---

## 소스맵에 원본이 통째로 들어 있었습니다

배포된 SDK의 소스맵을 열었더니 `sourcesContent` 에 **원본 TypeScript 전문**이 있었습니다

> `sourcesContent` 는 소스맵이 원본 소스를 문자열로 담는 선택 필드입니다, 많은 패키지가 켜 둡니다

**브라우저 소스맵에서 300여 파일 + Node 소스맵에서 몇 파일 + 타입 선언 70여 파일**

합쳐 400여 파일
갱신본 복원 스크립트가 각 단계의 파일 수를 확인합니다

로직을 다시 짤 필요가 없습니다
그 로직 자체를 씁니다
**버킷팅 해시를 비트 단위로 똑같이 재현해야 하는 위험을 피합니다**

핵심 도메인 로직은 유지하고, 조립, 엔트리, 의존성과 일부 유틸리티를 바꿨습니다

---

## 그런데 여기서 한 번 틀렸습니다

평가 엔진을 안 쓰는 소비자에겐 빼 주려고 플래그를 뒀습니다

```ts
// 안 빠진다. 플래그로 "만드느냐"만 막았을 뿐
static create({ evaluator }: { evaluator: boolean }) {
  if (evaluator) registry.register(new VariantEvaluator(...))
  //                                   ^^^^^^^^^^^^^^^^ 이 이름이 파일에 남아 있다
}
```

이 빌드에서는 플래그가 항상 `false`라고 접히지 않아 **평가 엔진 참조가 남았습니다**

빌드 시점에 조건을 확정할 수 있는 경우와 구분해야 합니다

---

## 기능별 조립을 다른 엔트리로 옮겼습니다

```ts
// Before: 코어가 평가 엔진을 import 해서 직접 조립한다
import {VariantEvaluator} from './evaluation/VariantEvaluator'
static create({evaluator}) { if (evaluator) registry.register(new VariantEvaluator()) }

// After: 조립은 바깥 모듈이 하고, 코어는 완성품만 받는다
static create(assembled: Assembled) { /* 이미 조립된 것을 받기만 */ }
```

조립 코드를 코어 바깥의 **별도 파일**로 옮기고 `create()` 를 뒤집었습니다

코어 파일의 평가 엔진 `import` 가 **0개**가 되고 나서야
타겟 매칭 코드가 실제로 빠졌습니다

**전송용 엔트리에서 평가 엔진으로 이어지는 import 경로를 끊었습니다**

---

## 결과: 엔트리, 빌드, 의존성을 바꾼 뒤

700줄짜리 단일 엔트리를 기능별로 나눠 엔트리 8개로 만들었습니다

| 가져가는 엔트리                              | gzip (KiB) | 벤더 대비  |
| -------------------------------------------- | ---------- | ---------- |
| **`./send`** (이벤트 전송만, 우리가 쓰는 것) | **약 30**  | **-68.9%** |
| `./eval` (A/B 평가까지)                      | 약 47      | -51.0%     |
| `.` (전체 엔트리)                            | 약 66      | -30.4%     |
| 벤더 원본                                    | 약 95      | 기준       |

**2026-09-05 갱신본**을 패키지 이름으로 import해 측정했습니다
**엔트리 분리, 빌드, 폴리필, 의존성 변경을 함께 반영한 결과**입니다

<!-- 초기 분석과 최종 크기는 버전과 호출 시나리오가 다르다. 동일 import의 전후 비교로 읽지 않도록 구분한다. 원값: send gzip 30,240 B, eval 47,664 B, 전체 67,614 B. KiB는 1,024 B. -->

---

<!-- _class: part invert -->

# 3. 라이브러리 대신 플랫폼

## 외부 런타임 의존성을 0으로 만들기까지

---

## 엔트리가 이렇게 폴리필을 심고 있었습니다

```ts
import 'core-js/features/promise'
import 'core-js/features/array'
```

> `features/*`로 가져온 범위에 실제로 쓰지 않는 메서드까지 포함되어 있었습니다
> **폴리필이 무엇을 추가하는지와 SDK가 무엇을 쓰는지를 따로 조사했습니다**

소스맵의 압축 전 원본 소스 기준으로 core-js 유래 코드가 약 200KB였습니다

제거 여부를 판단하기 위해 **실제 사용처와 필요한 실행 환경부터 조사했습니다**

---

## 쓰는 메서드와 필요한 플랫폼을 따로 봤습니다

| 조사 대상             | 결과                                        |
| --------------------- | ------------------------------------------- |
| Promise / async·await | 90회 이상, API 지원과 문법 타깃을 각각 확인 |
| `flat` / `flatMap`    | 3곳을 `reduce`로 치환                       |
| 추가 주입 메서드 10종 | 조사한 사용처 0회                           |

폴리필 import를 제거하고 **빌드 문법 타깃을 ES2017**로 맞췄습니다
다만 `TextEncoder`, `btoa`, Web Crypto는 실행 환경이 제공해야 합니다

**ES2017로 빌드했다는 사실만으로 브라우저 호환성이 보장되지는 않습니다**
실제 브라우저·웹뷰의 지원 하한과 실행 검증은 남아 있습니다

<!-- Node는 최소 20을 선언했으나 기록된 검증 환경은 Node 24.14.0이다. 실제 Chrome/Safari/Firefox에서의 전송 타이밍, visibilitychange, 쿠키 처리는 검증 범위 밖이다. -->

---

## base64도 다섯 줄, 단 원본과 대조합니다

```ts
// 우리 구현. 플랫폼 내장 TextEncoder와 btoa만 쓴다
export function encodeBase64Url(input: string): string {
  const bytes = new TextEncoder().encode(input) // btoa는 latin1 밖이면 예외
  let bin = ''
  for (const b of bytes) bin += String.fromCharCode(b)
  return btoa(bin).replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/, '')
}
```

이 결과가 **요청 헤더에 실려 서버가 파싱** 합니다, 한 바이트도 달라선 안 됩니다

```ts
import {Base64} from 'js-base64' // 걷어낸 원본 라이브러리. devDependency로만 남긴다
for (let n = 0; n <= 64; n++) {
  expect(encodeBase64Url('x'.repeat(n))) // 우리 구현
    .toBe(Base64.encodeURL('x'.repeat(n))) // 원본 라이브러리
}
// 0~64는 패딩 경계(길이 % 3) 전수. 시드 고정 난수 유니코드 2000건도 같은 방식으로 대조
```

---

## grep으로 의존성을 예측했다가 또 틀렸습니다

base64 라이브러리를 걷어내면서 `grep` 결과를 보고 **"`./send` 엔트리에는 영향 없을 것이다"** 라고 예측했습니다
그런데 번들 그래프에는 **전송 엔트리에서 base64로 이어지는 경로**가 있었습니다

```bash
$ grep -rl base64 src                    # 파일 목록으로 본 것
src/core/internal/util/base64.ts
src/core/internal/user/CohortFetcher.ts
src/core/internal/user/TargetFetcher.ts  # 엔트리 파일이 없으니 send와 무관하다고 봤다
```

```bash
$ esbuild src/index.send.ts --bundle --metafile=meta.json   # 번들 그래프로 본 것
index.send.ts → … → TargetFetcher.ts → base64.ts
# metafile의 inputs/imports를 따라가 확인한 경로 (중간 모듈 생략)
```

지금은 번들 산출물의 metafile에서 **엔트리까지 이어지는 import 경로**를 확인합니다

<!--
2026-09-15 ~/works/hackle-sdk 현재 상태에서 실측. esbuild src/index.track.ts --bundle --format=esm --minify --metafile 로 metafile을 뽑고, inputs 그래프를 BFS로 따라가 base64.ts까지의 경로를 출력한 것. 슬라이드의 send/CohortFetcher/TargetFetcher는 track/UserCohortFetcher/UserTargetFetcher를 익명화한 이름.
base64.ts를 import하는 파일은 UserTargetFetcher, UserCohortFetcher 둘. 번들 내 바이트: base64.ts 181 B, 두 fetcher 합 1,332 B, 번들 전체 112,499 B.
-5.1%는 js-base64 제거 당시(초기 커밋 이전) 기록이라 지금 저장소로는 재현 불가. 최종 68.9%와 합산하지 않는다.
-->

---

<!-- _class: part invert -->

# 4. 같은 동작인지 어떻게 확인하나

## 조립, 엔트리, 의존성을 전부 바꿨습니다

---

## 기대값을 직접 쓰는 대신 원본과 비교했습니다

대조할 것은 **실험군 배정 결과**와 **이벤트의 비교 대상 필드**입니다

```ts
// 벤더 배포본은 격리한 jsdom에서 실행
w.eval(readFileSync(VENDOR_UMD, 'utf8'))

// 우리 소스에는 같은 입력을 전달
const ours = await import('../../src/index.browser')
compare(clients, (c) => c.evaluate(userId, flagKey))
```

**가짜 유저 40명과 조건별 실험 설정을 같은 입력으로 넣고, 평가 결과를 대조했습니다**
이벤트는 비교할 필드를 정하고, 난수·시각과 의도적 차이는 따로 다뤘습니다

소스 대조에 더해, **빌드된 패키지의 8개 엔트리도 초기화·결정·이벤트를 비교**합니다

<!-- 코드 이름은 익명화한 구조 예시다. 빌드 스모크는 엔트리별 대표 시나리오이며 전체 API 검증이 아니다. 전송용 엔트리의 internalProperties 차이는 양쪽 값을 별도로 단언하고 나머지 필드를 비교한다. -->

---

<!-- _class: punch -->

# 당시 테스트는 모두 통과했습니다

## 대조 대상으로 삼은 값의 불일치 0건

그런데 그중 몇 개는, 의도한 동작을 확인하지 못하고 있었습니다

<!-- 한 박자 쉰다. 최종 테스트 수를 실패 발견 전의 수치로 소급하지 않는다. -->

---

## 그래서 일부러 망가뜨려 봤습니다

테스트가 변경을 검출하는지 확인하려고 **소스를 일부러 바꾸고, 어떤 테스트가 실패하는지 확인했습니다**
이를 **뮤테이션 테스트**라고 합니다

```js
{
  find:    'bucketHash(value, seed)',       // 유저를 A군, B군으로 나누는 해시
  replace: 'bucketHash(value, seed + 1)',   // 시드를 1 올리면 일부 유저의 배정이 바뀐다
  detects: '평가 결과 대조 테스트'           // 그러니 이 테스트가 실패해야 정상
}
```

안 걸렸다면 **현재 입력과 단언으로 그 변경을 검출하지 못했다**는 뜻입니다
코드에 도달하지 않았거나, 경계 입력·단언이 부족하거나, 결과가 같은 변경일 수 있습니다

테스트 실행 오류는 검출과 구분합니다
분류 기준은 자바스크립트 뮤테이션 테스트 도구인 [Stryker](https://stryker-mutator.io/docs/mutation-testing-elements/mutant-states-and-metrics/)의 문서를 따랐습니다

<!-- 출처: Stryker mutant states와 equivalent mutants 문서. 이번 검토에서 검사기의 crashed || failed > 0 집계를 수정해 실행 오류를 분리했다. 고의 변경 선택과 실제 검출 테스트는 내부 메모의 기준 커밋·실행 기록에 남긴다. -->

---

## 한 곳씩 망가뜨리고 전체 테스트를 돌립니다

소스 한 곳을 바꾸고, **약 2천 개의 테스트**를 돌리고, 되돌립니다
선택한 고의 변경은 약 100개입니다 결과는 세 가지로 나눕니다

| 결과      | 해석                                                 |
| --------- | ---------------------------------------------------- |
| 검출      | 실제 테스트 단언이 실패함, 실패한 테스트 이름을 확인 |
| 미검출    | 현재 입력과 단언이 이 변경을 구별하지 못함           |
| 실행 오류 | 테스트 실행·수집·리포트 오류, 검출로 집계하지 않음   |

**처음에는 안 걸리는 것들이 있었습니다**
그 원인을 추적했습니다
고의 변경은 제가 고른 지점입니다 떠올리지 못한 결함까지 검사했다는 뜻은 아닙니다

<!-- 공개 수치는 의도적으로 약 2천 테스트·약 100개 고의 변경으로 표현한다. 기준은 GitHub d894381(벤더 12.1.1)의 1,998개 / 27파일, 102개 고의 변경이며, 이전 로컬 d34e813의 1,948개 / 80개와 구분한다.
검사기는 기준선 통과·대상 문자열 1회 매칭·finally 및 시그널 핸들러에서 원복을 확인한다. 실행 오류는 검출로 세지 않도록 수정했다. 2026-09-15 Node 24.20.0에서 수정된 검사기로 기준선 1,998개 통과, 102/102 검출, 실행 오류 0건을 확인했다. 각 검출에 실제 단언 실패가 있으며 개별 로그는 내부 메모에 기록한다.
처음 안 걸린 사례는 README의 URL 분기, IN에 편중된 픽스처, DELTA 병합 테스트다. 뒤의 키 충돌은 픽스처 확장 도중 생긴 별개 사고다. -->

---

## URL 판정을 망가뜨려도 통과했습니다

이 SDK에는 "URL이 조건에 맞으면 다른 페이지로 보낸다"는 기능이 있습니다
그 판정 함수를 **무조건 true**로 바꿨는데, "조건에 안 맞으면 보내지 않는다"를 확인하는 테스트가 그대로 통과했습니다

원인은 테스트끼리 격리가 안 된 것이었습니다

```ts
// SDK가 켜질 때, 브라우저의 pushState를 자기 함수로 한 겹 감싼다
const original = history.pushState
history.pushState = function (...args) {
  original.apply(this, args)
  notifyUrlChange()
}
// 그런데 close()는 이 겹을 벗기지 않는다
```

**앞 테스트에서 만든 SDK가 `close()` 뒤에도 남아서, 계속 URL 변화를 듣고 있었습니다**

---

## 앞 테스트의 SDK가 판정을 가로막았습니다

앞 테스트가 남긴 SDK를 **좀비**, 이번 테스트가 만든 SDK를 **이번 SDK**라고 부르겠습니다

1. 이번 테스트가 시작하며 URL을 바꿉니다, 좀비도 그 신호를 받습니다
2. 좀비는 망가진 판정문(무조건 true) 때문에 리다이렉트를 실행하고, "이미 보냈음" 표시(쿠키)를 남깁니다
3. 이번 SDK는 그 표시를 보고 **아무것도 하지 않고 끝냅니다**, 판정문을 타지 않으니 망가진 줄도 모릅니다
4. 테스트는 "이번 SDK가 리다이렉트하지 않았다"만 확인하므로 **통과**합니다

**통과한 이유가 "코드가 맞아서"가 아니라 "앞 테스트가 남긴 상태 때문"이었습니다**

원본 소스에서 전역 패치를 찾아 **테스트 부팅 전에 네이티브 history 함수를 복원했습니다**
이후 같은 고의 변경을 넣어 실패하는지 확인했습니다

<!-- 수정: SDK를 켜기 전에 원래 pushState로 되돌려 좀비를 떼어 낸다. 블로그 "원본에서만 우연히 옳던 테스트" 절. 하네스는 모듈 로드 시점에 네이티브 pushState/replaceState를 캡처해 두고 부팅 진입 시 복원한다. close()가 패치를 안 되돌리는 것은 벤더와 동일한 동작이라 프로덕션 코드는 그대로. 수정 전후 고의 변경 로그는 발표 전에 확보한다. -->

---

## 비교 연산자를 바꿔도 통과했습니다

타겟 조건은 "나이 > 25"처럼 연산자로 유저를 거릅니다
그 `>`를 `>=`로, "포함"을 "시작"으로 바꿨는데 **아무 테스트도 실패하지 않았습니다**

테스트 입력에 넣은 타겟 조건이 **전부 `IN` 하나**였기 때문입니다
유저 40명을 전수 대조했지만, 실은 같은 길만 40번 밟고 있었습니다

| 빠진 경로        | 추가한 입력                   |
| ---------------- | ----------------------------- |
| `>`와 `>=`       | 기준값과 정확히 같은 값       |
| 포함과 시작 매칭 | 문자열이 중간에만 나타나는 값 |

연산자별 실험 12개를 테스트 입력에 추가해 **원본과 대조하는 경로를 넓혔습니다**
그리고 연산자를 다시 바꿔 넣어 이제는 걸리는지 확인합니다

<!-- 블로그 "픽스처가 연산자 하나만 쓰고 있었다" 절. 최종 갱신본은 연산자 9종과 valueType 조합까지 확장했다(README 커버리지 매트릭스). 이전 문서의 40명 × 24개 = 480쌍은 오류라 쓰지 않는다. -->

---

## 같은 입력 오류가 양쪽 실험을 지웠습니다

앞 장의 연산자 테스트용 실험을 테스트 입력에 추가하다가, **이미 쓰이고 있던 번호를 새 실험에 다시 붙였습니다**
그러자 잘 잡히던 고장들이 갑자기 안 잡히기 시작했습니다

1. 실험 설정은 번호를 이름표로 맵에 담깁니다, 번호가 겹치면 뒤 실험이 앞 실험을 조용히 덮습니다
2. 원래 그 번호를 쓰던 실험 셋이 테스트 입력에서 사라졌습니다, 에러는 없습니다
3. 양쪽 SDK가 똑같이 "그런 실험 없음"을 돌려주니 비교 결과는 **일치**입니다

**이 입력에서는 양쪽의 "실험 없음"이 일치했습니다, 출력 일치만 검사하면 놓칩니다**
원본을 정답지로 쓰더라도, 문제지에 실험이 실제로 들어 있는지 확인해야 합니다

그래서 테스트 입력 자체를 검사하는 테스트를 뒀습니다
번호가 겹치면 그 자리에서 실패합니다

<!-- 블로그 "픽스처 키가 겹쳐 실험이 사라졌다" 절. 실험 상태 필드에 잘못된 코드를 넣어 초안/종료 실험이 조용히 누락된 사례도 같은 종류다. 픽스처 검사 테스트는 test/workspace-fixture.test.ts. -->

---

<!-- _class: you -->

## SDK 테스트에서 먼저 확인할 것

| 이 발표에서 놓친 것       | 여러분의 테스트에서 확인할 것                             |
| ------------------------- | --------------------------------------------------------- |
| 앞 테스트의 SDK가 남음    | 종료 후 전역 패치·타이머·스토리지까지 격리됐는가          |
| 판정문에 도달하지 않음    | 준비 완료와 정상 동작을 확인한 뒤, 부정 조건을 검사하는가 |
| 입력이 한 경로에 편중됨   | 경계값·비매치 입력과 설정 키의 유효성을 검사하는가        |
| 양쪽이 같은 기본값을 반환 | 출력 일치 외에 기대한 상태·이유까지 단언하는가            |

**"호출되지 않았다"만으로 올바른 분기를 탔다고 판단하지 마세요**
SDK가 준비되지 않았거나 설정을 못 읽어도 똑같이 호출되지 않습니다

<!-- 일반화 근거: URL 분기 격리 실패, IN에 편중된 입력, 실험 키 충돌 사례. 종료 함수 호출이나 mock 초기화만으로 전역 원복이 완료됐다고 가정하지 않는다. 정상·부정 조건은 각각 격리된 환경에서 확인하며, 한 테스트의 상태로 다른 테스트의 사전 조건을 만들지 않는다. -->

---

<!-- _class: you -->

## 고장은 SDK 바깥에서도 넣을 수 있습니다

**앱 단위 테스트:** SDK 래퍼를 mock해 성공·오류·지연에 대한 앱의 반응을 확인합니다
**연동 테스트:** 버전을 고정한 실제 SDK로 준비·호출·전송 계약을 확인합니다

정상 동작을 기대하는 테스트에, 다음 변경을 하나씩 넣어 봅니다

| 일부러 바꿀 것                       | 실패해야 할 검사                     |
| ------------------------------------ | ------------------------------------ |
| 전송 함수를 아무 일도 안 하게 만들기 | 이벤트가 전송 경계에 도달했는지      |
| 입력에서 필요한 설정 하나 빼기       | 기대한 실험·분기를 실제로 선택했는지 |

**단독·전체·순서를 바꾼 실행에서 결과가 달라지면 공유 상태부터 추적합니다**
mock과 원본이 다른 계약을 갖지 않도록, 실제 SDK를 쓰는 검사도 남깁니다

<!-- 청중 적용 예시이며 이번 SDK에서 모두 실행한 실험이라는 뜻은 아니다. 전송 경계의 호출을 확인한 것과 서버 수집·집계 성공은 구분한다. 랜덤·시각은 제어하거나 해당 필드의 형식을 별도 단언하고, 이벤트 전체를 무분별하게 비교 제외하지 않는다. -->

---

## 전송은 어디까지 확인했나

| 확인한 것                     | 근거                                |
| ----------------------------- | ----------------------------------- |
| 이벤트의 비교 대상 필드       | 원본 UMD와 같은 입력으로 대조       |
| 전송 엔트리의 메타데이터 차이 | 원본 값과 빈 객체를 각각 단언       |
| 엔트리별 대표 동작            | 빌드한 패키지를 격리 jsdom에서 대조 |

**실제 웹뷰의 전송·이탈 처리와 서버 수집·대시보드 집계는 별도 검증이 필요합니다**
메타데이터 차이를 서비스에서 허용할지는 그 결과로 판단해야 합니다

<!-- 근거: 기준 커밋의 test/built/built-bundle.test.ts, README 엔트리 계약과 sandbox PoC 항목. 실제 배포한 산출물·수집 결과와 기준 커밋의 대응은 확보하지 못했다. 평가 엔진 대조가 전송의 실환경 검증을 대신한다고 설명하지 않는다. -->

---

## 작아진 SDK는 직접 유지해야 합니다

**버전이 바뀌면 로직·전송 계약·검증 입력도 다시 확인해야 합니다**

- 검증용 벤더 배포물은 소스를 덮어쓰지 않고 별도로 준비합니다
- 소스 대조와 뮤테이션 테스트에 더해, 빌드 산출물·타입 소비·크기를 검사합니다
- 재추출은 수정한 소스를 덮어씁니다
  갱신 작업을 자동으로 끝내 주지 않습니다

**갱신 비용과 운영 검증, 원본으로 복귀하는 방법까지 교체 비용에 포함해야 합니다**
이 발표에서 확보한 근거는 재구성과 코드·번들 검증까지입니다

<!-- 실제 근거: package.json의 verify 순서, tools/extract-sources.mjs의 --vendor-only와 --force 가드, README 갱신 규칙. 갱신 소요 시간·담당자·복귀 절차를 운영 실적으로 제시할 자료는 없다. -->

---

<!-- _class: punch -->

# 전송에 필요한 코드만 가져옵니다

```ts
// 벤더 엔트리: gzip 약 95KiB
import {createClient} from '@vendor/js-sdk'

// 재구성한 전송용 엔트리: gzip 약 30KiB
import {createClient} from '@rebuilt/js-sdk/send'
```

## 전송용 번들 약 69% 감소

원본과 대조하고, 일부러 바꾸고, 놓친 경로를 확인했습니다

<!-- 화면 수치는 의도적으로 반올림한다. 초기 상수 import의 91KiB를 최종 비교에 섞지 않는다. 2026-09-15 동일한 초기화·track·사용자 ID·기기 ID 호출을 패키지 이름으로 소비해 재측정했다. 벤더 12.1.1 gzip 97,167B, 갱신본 전송용 30,241B, 감소율 약 68.88%. esbuild 0.24.2, browser/esm/es2017, gzip level 9. 정확한 명령과 로그는 내부 발표 메모에 남긴다. -->

---

<!-- _class: lead invert -->

# 감사합니다

<br>

궁금한 점이 있으시면 편하게 말씀해 주세요

---

## 백업: 홈 전체 최적화의 관측 결과

**SDK 교체·로거 제거·지연 로딩·의존성 정리를 함께 적용했습니다**
SDK 단독 기여분은 분리 측정하지 않았습니다

| 지표                 | before   | after    | 변화    |
| -------------------- | -------- | -------- | ------- |
| 첫 콘텐츠 표시 (FCP) | 약 1.1초 | 약 0.6초 | 약 -44% |
| 전송량 (압축 후)     | 약 1.1MB | 약 0.7MB | 약 -32% |

모바일 에뮬레이션·CPU 4배 감속·콜드 로드
같은 sandbox에 순차 배포한 before 4회·after 5회의 중앙값입니다

개별 측정의 분산과 주요 콘텐츠·조작 가능 시점의 개선은 이 표로 판단할 수 없습니다

<!-- 출처: 사내 home-atf-performance.pdf 36쪽(1부터). FCP 1,119→622ms, 전송량 1.06→0.72MB. CPU 행은 집계 정의를 확인하지 못해 제외했다. SDK 단독 기여·실기기 결과로 읽지 않는다. 37쪽의 19회 측정은 위 9회와의 관계가 불명확해 사용하지 않는다. -->

---

## 백업: UUID 형식을 유지하며 바꿨습니다

```ts
export function v4(): string {
  const c = globalThis.crypto
  if (typeof c.randomUUID === 'function') return c.randomUUID()

  // 없으면 직접 조립한다: 랜덤 16바이트를 받아서
  const b = new Uint8Array(16)
  c.getRandomValues(b)
  b[6] = (b[6] & 0x0f) | 0x40 // 7번째 바이트 상위 4비트 = 버전(4)
  b[8] = (b[8] & 0x3f) | 0x80 // 9번째 바이트 상위 2비트 = 변형(RFC 4122)
  /* ...hex 문자열로 조립... */
}
```

**왜 폴백이 필요한가?** `randomUUID` 에는 `[SecureContext]` 제약이 있습니다
보안 컨텍스트가 아닌 환경에서는 사용할 수 없어, `getRandomValues` 기반 폴백을 뒀습니다

---

## 백업: 여기서 함정이 둘 있었습니다

**지원 버전뿐 아니라 실행 환경의 제약까지 확인해야 합니다**
MDN 본문과 스펙에 secure context 제약이 명시되어 있습니다
사용 환경에서 `crypto.getRandomValues`도 제공되는지 확인해야 합니다

<br>

**"더 가벼운 라이브러리로 바꾸자"도 답이 아니었습니다**
`nanoid` 안이 있었는데, 세션 ID가 `uuid.slice(0, 8)` 형식이었습니다
uuid는 소문자 16진수라 앞 8자가 `a3f9c1d2` 같은 모양인데,
기본 nanoid는 문자 집합이 달라서 **그대로 교체하면 기존 형식 계약이 바뀝니다**

<br>

이 작업에서는 **플랫폼 내장과 UUID 형식을 유지하는 폴백**을 선택했습니다

---

## 백업: 출처와 측정 범위

- **트리셰이킹·PURE:** [esbuild 공식 문서](https://esbuild.github.io/api/#tree-shaking)
- **결과 분류와 동등한 변경:** [Stryker 문서](https://stryker-mutator.io/docs/mutation-testing-elements/mutant-states-and-metrics/), [동등한 변경 예시](https://stryker-mutator.io/docs/stryker-js/disable-mutants/)
- **UUID 실행 환경:** [MDN randomUUID](https://developer.mozilla.org/en-US/docs/Web/API/Crypto/randomUUID), [getRandomValues](https://developer.mozilla.org/en-US/docs/Web/API/Crypto/getRandomValues)
- **초기 진단·실패 사례:** [작업 기록](https://yceffort.kr/2026/08/rebuilding-a-vendor-sdk)

크기는 초기 진단과 갱신본의 패키지 소비 측정을 구분했습니다
화면 수치는 대략값이며, 테스트는 준비한 입력과 실행 환경의 결과입니다

<!-- 구현·크기·검증의 정확한 출처는 notes/feconf-2026-plan.md의 기준 커밋과 근거 표에 기록한다. 공개 화면에는 역할 중심 가명을 유지한다. 사내 성능 자료는 홈 전체 최적화의 관측값으로만 사용한다. -->
