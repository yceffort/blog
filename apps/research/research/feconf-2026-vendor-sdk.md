---
title: '외부 SDK를 뜯어 다시 만들었습니다: 전송용 번들 gzip 크기를 69% 줄이고, 검증의 빈틈 찾기'
marp: true
paginate: true
theme: feconf
header: 'FECONF 2026 / FRONTEND ENGINEERING'
footer: '카카오페이증권  /  김용찬'
transition: glide
tags:
  - feconf
  - bundler
  - tree-shaking
  - testing
  - sdk
date: 2026-09-15
description: '상수 하나를 import했는데 gzip 90KiB대가 따라왔습니다. 원인을 실험으로 가르고, 배포된 소스맵과 타입 선언에서 SDK를 복원해 전송용 번들을 raw 74%, gzip 69% 줄였습니다. 조건 분기로 제거되지 않던 참조를 끊고, 원본 대조와 고의 변경으로 검증의 빈틈을 찾은 과정을 공유합니다.'
published: false
art:
  undraw: app-benchmarks
---

<!-- _class: cover -->

<!-- _header: FECONF 2026 / KAKAOPAY SECURITIES -->

# 외부 SDK를 뜯어<br>다시 만들었습니다

## 전송용 번들 gzip 크기를 69% 줄이고,<br>검증의 빈틈을 찾기까지

> **−69%**
>
> 전송용 번들 / gzip

**김용찬** 카카오페이증권 프론트엔드 엔지니어

<!--
발표 15초 / 00:00~00:15

안녕하세요. 카카오페이증권 프론트엔드 개발자 김용찬입니다. 오늘은 외부 SDK를 재구성해 번들을 줄이고, 그 과정에서 테스트의 빈틈을 찾은 이야기를 하겠습니다.
-->

---

<!-- _class: profile -->

<!-- _header: FECONF 2026 / PROLOGUE -->

## 사용자가 처음 만나는 화면을 만듭니다

### 김용찬

카카오페이증권 / 홈팀 프론트엔드 엔지니어

- 『모던 리액트 Deep Dive』, 『npm Deep Dive』, 『프런트엔드 성능 최적화 Deep Dive』 저자
- DAN 24 연사

_[yceffort.kr](https://yceffort.kr) / [github.com/yceffort](https://github.com/yceffort)_

<!--
발표 35초 / 00:15~00:50

저는 사용자가 앱을 켜면 가장 먼저 보는 증권홈 화면을 개발하고 있습니다.

화면에 보이는 세 권의 책을 썼고, 2024년 DAN에서도 발표했습니다.
-->

---

<!-- _class: code-focus -->

<!-- _header: FECONF 2026 / PROLOGUE -->

## 시작은, 상수 하나였습니다

```ts
import {Status} from '@vendor/sdk'

console.log(Status.DEFAULT)
```

SDK 초기화도, 함수 호출도 하지 않았습니다

> **문자열 상수 하나를 읽었을 뿐인데, 파일이 무거웠습니다**

_패키지와 API 이름은 설명용 가명입니다. 측정값과 실패의 원인 관계는 원래 기록을 유지했습니다_

<!--
발표 45초 / 00:50~01:35

먼저 말씀드리면, 발표에 나오는 패키지와 API 이름은 모두 설명용 가명입니다. 측정값과 실패의 원인 관계는 실제 기록 그대로입니다.

당시 저희가 쓰던 외부 SDK는 다른 것으로 대체할 수 없어 계속 사용해야 했습니다. 그래서 필요한 동작을 유지하면서 번들을 줄일 방법을 찾아야 했습니다.

그 출발점이 화면의 두 줄입니다. SDK에서 상수 하나를 가져와 출력했습니다. SDK를 초기화하지도, SDK 함수를 호출하지도 않았습니다. 문자열 상수 몇 개가 들어 있는 열거형에서 하나를 읽었을 뿐인데, 번들이 이상하게 무거웠습니다.
-->

---

<!-- _class: columns -->

<!-- _header: FECONF 2026 / PROLOGUE -->

## 필요한 것보다 많은 코드가 따라왔습니다

> ### 홈에서 필요한 것
>
> **데이터 전송**
>
> 사용자가 보내는 데이터를 전달하는 경로

<!-- -->

> ### SDK 초기화에 연결된 것
>
> 데이터 전송 + **선택 기능 A + B**
>
> 사용하지 않는 기능의 조립까지 연결

기능의 사용 범위와 **초기화 경로의 참조 범위**가 달랐습니다

<!--
발표 25초 / 01:35~02:00

먼저 이 SDK에서 저희가 쓰는 기능을 보겠습니다. 홈에 필요한 것은 왼쪽의 데이터 전송 하나입니다. 그런데 오른쪽 초기화 경로에는 쓰지 않는 선택 기능 두 개의 조립까지 연결돼 있었습니다. 쓰는 기능보다 따라오는 코드의 범위가 넓었습니다.
-->

---

<!-- _class: signal -->

<!-- _header: FECONF 2026 / PROLOGUE -->

## 상수 하나가 실사용만큼 무거웠습니다

> **90 KiB대**
>
> 상수 하나를 import한 번들의 gzip

| 시나리오         | raw       | gzip     |
| ---------------- | --------- | -------- |
| 상수 하나 import | 374.7 KiB | 91.4 KiB |
| 실제 초기화      | 383.5 KiB | 94.6 KiB |

상수 하나만 가져와도 **raw 기준 실사용 번들의 98%**가 남았습니다
크기를 줄이되, **필요한 동작은 유지해야 했습니다**

_초기 분석 스냅샷 / esbuild 0.24 / gzip 레벨 9. 98%는 두 번들의 raw 크기 비율입니다_

<!--
발표 40초 / 02:00~02:40

그 차이를 크기로 확인하려고, 상수만 가져오는 경우와 실제로 초기화하는 경우를 비교했습니다. 상수만 가져온 번들이 raw 약 375 KiB, gzip 약 91 KiB입니다. 초기화하는 번들과 비교해도 raw 기준 98%가 남았습니다.

상수만 쓴다면 나머지는 번들러가 빼 줄 거라고 기대했는데, 실제로는 거의 그대로였습니다. 왜 코드가 남는지 알아야, 필요한 동작을 유지하면서 줄일 수 있었습니다.
-->

---

<!-- _class: chapter -->

<!-- _header: 01 / DIAGNOSIS -->

# 왜 안 줄어드는가

## 원인에 따라 해결 방법이 달라집니다

> 01

<!--
발표 10초 / 02:40~02:50

상수만 쓰는데 왜 대부분의 코드가 남았을까요? 번들러가 코드를 제거하지 못하는 이유를 두 가지로 나눠 봤습니다.
-->

---

<!-- _class: columns -->

<!-- _header: 01 / DIAGNOSIS -->

## 지워도 되는가, 실제로 쓰이는가

> ### 01 / 안전성
>
> **번들러가 확신을 못 한다**
>
> 클래스가 IIFE로 바뀌면서<br>호출을 지워도 안전한지 판단하기 어렵습니다
>
> `sideEffects` → 모듈 단위<br>[PURE](https://esbuild.github.io/api/#pure) → 호출 단위의 힌트

<!-- -->

> ### 02 / 도달 가능성
>
> **초기화 경로에서 실제로 참조한다**
>
> 쓰지 않는 선택 기능도<br>초기화 경로에 연결되어 있습니다
>
> 힌트를 넘어<br>**코드 구조를 바꿔야 합니다**

_트리셰이킹: 번들러가 사용되지 않는다고 판단한 코드를 제거하는 최적화_

<!--
발표 70초 / 02:50~04:00

이때 기대한 최적화가 트리셰이킹입니다. 번들러가 사용하지 않는 코드를 빼 주는 기능인데, 여기서는 두 가지가 걸렸습니다.

첫째는 번들러가 지워도 안전한지 확신하지 못하는 경우입니다. 클래스가 즉시 실행 함수로 바뀌어 있으면, 그 함수가 실행될 때 다른 상태를 바꾸는지 판단하기 어려울 수 있습니다. sideEffects는 모듈 단위로, PURE 주석은 호출 단위로 제거를 도울 정보를 줍니다.

둘째는 초기화 경로에서 다른 기능을 실제로 참조하는 경우입니다. 실행될 수 있는 코드이기 때문에, 사용하지 않는 코드로 판단할 수 없습니다.

첫 번째라면 올바른 힌트가 도움이 될 수 있습니다. 두 번째라면 어떤 기능이 연결되는지, 그 구조를 바꿔야 합니다. 그래서 두 경우를 나눠서 실험했습니다.
-->

---

<!-- _class: compact -->

<!-- _header: 01 / DIAGNOSIS -->

## 같은 PURE 힌트, 다른 결과

```js
// 이 호출은 부수효과가 없으니, 안 쓰면 지워도 된다는 힌트
const module = /*#__PURE__*/ createModule()
```

배포본의 클래스 IIFE에 [PURE 주석](https://esbuild.github.io/api/#pure)을 주입해 비교했습니다

| 시나리오                     | 원본 gzip | PURE 주입 후 | 변화        |
| ---------------------------- | --------- | ------------ | ----------- |
| 상수 하나만 import           | 91.4 KiB  | 24.2 KiB     | **약 −74%** |
| 실사용 (`createClient` 호출) | 94.6 KiB  | 93.4 KiB     | **약 −1%**  |

> 힌트는 제거를 허용합니다. **제거해도 안전한지는 따로 검증해야 합니다**

_초기 분석 스냅샷의 실험. 최종 갱신본의 측정과 구분합니다_

<!--
발표 60초 / 04:00~05:00

먼저 제거해도 된다는 힌트의 영향을 확인했습니다. 배포본에서 클래스를 감싼 즉시 실행 함수에 PURE 주석을 넣고, 두 시나리오를 다시 빌드했습니다.

상수만 가져오는 첫 번째 행은 gzip 크기가 약 74% 줄었습니다. 반면 실제로 초기화하는 두 번째 행은 약 1%만 줄었습니다. 같은 힌트를 넣어도, 무엇을 사용하는지에 따라 결과가 크게 갈렸습니다.

다만 PURE 주석은 개발자가 번들러에 주는 정보입니다. 주석을 붙여서 코드가 빠졌다는 사실만으로 그 변경이 안전하다고 할 수는 없습니다. 동작 검증은 뒤에서 따로 하겠습니다.
-->

---

<!-- _class: columns contrast -->

<!-- _header: 01 / DIAGNOSIS -->

## 힌트만으로는 실사용 번들이 줄지 않았습니다

> 상수만 import
>
> **−74%**
>
> 부수효과 힌트가<br>제거 여부에 큰 영향을 줬습니다

<!-- -->

> 실제 초기화
>
> **−1%**
>
> 여러 기능의 참조가 남아<br>같은 힌트로는 줄지 않았습니다

**실사용 번들을 줄이려면 초기화 구조와 엔트리까지 바꿔야 했습니다**

_PURE 주입 실험 / 초기 분석 스냅샷 / gzip 감소율_

<!--
발표 35초 / 05:00~05:35

저희가 집중해야 할 쪽은 오른쪽입니다. 상수만 가져오는 예제는 크게 줄었지만, 실제로 SDK를 초기화해서 쓰면 효과가 거의 없었습니다.

홈에서는 초기화와 전송이 필요합니다. 그래서 최소 예제의 크기만 줄여서는 문제가 해결되지 않았습니다. 실제 초기화에서 여러 기능으로 이어지는 참조를 바꿔야 했습니다.
-->

---

<!-- _class: evidence -->

<!-- _header: 01 / DIAGNOSIS -->

## 실제 앱에서도, 이만큼을 받고 있었습니다

![SDK 공유 청크의 source-map-explorer 분석. 선택 기능과 공통 모듈이 넓은 영역을 차지합니다.](/feconf-2026/source-map-explorer.png)

Next.js 앱에서 **상수만 가져온 페이지가 받은 공유 청크**입니다

_최소 import와 초기화 라우트가 한 빌드 안에서 공유한 청크. 앞의 esbuild 실험과는 별도 관측입니다_

<!--
발표 50초 / 05:35~06:25

이 코드들이 앱에서는 어떻게 묶이는지도 확인했습니다. 이 그림은 상수만 가져오는 페이지가 받은 SDK 공유 청크입니다. 쓰지 않는 선택 기능과 공통 모듈이 넓은 영역을 차지하고 있습니다.

이 빌드에는 상수만 쓰는 페이지와 SDK를 초기화하는 페이지가 함께 있고, 둘이 같은 청크를 공유합니다. 앞의 esbuild 실험과 조건은 다릅니다. 여기서는 이 앱 구성에서 실제로 어떤 파일을 받는지 확인한 것입니다. 줄여야 할 코드가 어디에 묶여 있는지도 눈으로 볼 수 있었습니다.
-->

---

<!-- _class: chapter -->

<!-- _header: 02 / RECONSTRUCTION -->

# 원본 로직은 유지하고,<br>조립을 바꾸기로 했습니다

## 전송 경로를 따로 조립할 수 있을까?

필요한 동작을 유지하려면 **원본 로직을 확보하고, 재구성 전후를 대조해야 했습니다**

> 02

<!--
발표 60초 / 06:25~07:25

이제 필요한 전송 경로만 따로 조립하는 방향으로 가 보기로 했습니다.

다만 핵심 로직을 새로 구현하는 것은 부담이 컸습니다. 제가 이해한 대로 다시 만들면 같은 입력을 넣어도 처리 결과가 조용히 달라질 수 있습니다. 에러가 나면 알아채지만, 값이 미묘하게 달라지면 한참 뒤에 발견할 수도 있습니다.

그러려면 먼저 원본 로직을 확보해야 했습니다. 그 코드를 바탕으로 조립을 바꾸고, 변경 전후의 결과를 대조하기로 했습니다.
-->

---

<!-- _class: recovery -->

<!-- _header: 02 / RECONSTRUCTION -->

## 소스맵 안에 원본 TypeScript가 있었습니다

`sourcesContent`에 **원본 코드 전문**이 포함되어 있었습니다

- **브라우저 소스맵**<br>브라우저 구현
- **Node 소스맵**<br>함께 복원할 구현
- **타입 선언**<br>빠진 타입 보충

> 수백 개 파일을 복원했습니다. **핵심 알고리즘을 처음부터 다시 구현하지 않아도 됐습니다**

유지한 것: 핵심 도메인 로직<br>바꾼 것: 조립, 엔트리, 의존성, 일부 유틸리티

_복원 스크립트가 단계별 파일 수를 확인합니다. sourcesContent는 선택 필드입니다_

<!--
발표 60초 / 07:25~08:25

원본을 찾은 곳은 SDK와 함께 배포된 소스맵이었습니다. sourcesContent에 원본 TypeScript가 통째로 들어 있었습니다. sourcesContent는 소스맵에 원본 소스를 문자열로 담는 선택 필드입니다. 브라우저 소스맵과 Node 소스맵, 여기에 타입 선언까지 합쳐서 수백 개 파일을 복원했습니다.

이 코드를 확보한 덕분에 핵심 알고리즘을 처음부터 다시 구현할 필요가 없어졌습니다. 핵심 도메인 로직은 유지하고, 조립과 엔트리, 의존성, 일부 유틸리티를 바꿀 수 있는 출발점이 생겼습니다.
-->

---

<!-- _class: process compact -->

<!-- _header: 02 / RECONSTRUCTION -->

## 원본 소스를 꺼내는 순서

1. **빌드의 맵 찾기**<br>배포 JS의<br>`sourceMappingURL`
2. **같은 인덱스 연결**<br>`sources`와<br>`sourcesContent`
3. **파일로 복원하기**<br>경로를 정리하고<br>빠진 타입은 `.d.ts`로 보충

```bash
# 모든 원본 내용이 포함된 맵에서 추출하는 도구 예시
npx shuji@0.8.0 sdk.js.map -o restored -p
```

**`sourcesContent`가 없거나 `null`이면 원본을 별도로 확보해야 합니다**
`mappings`의 위치 정보만으로 코드 전문을 되살릴 수는 없습니다

_[shuji 사용법](https://github.com/paazmaya/shuji#command-line-options) / [소스맵 규격(ECMA-426)](https://tc39.es/ecma426/#sec-source-map-format)_

<!--
발표 75초 / 08:25~09:40

꺼내는 순서는 이렇습니다. 배포된 JavaScript의 sourceMappingURL을 따라 소스맵을 찾습니다. 그 안의 sources에 있는 파일 이름과 sourcesContent에 있는 내용을 같은 인덱스끼리 연결합니다. 경로를 정리해 파일로 저장하고, 빠진 타입은 배포된 타입 선언으로 보충했습니다.

화면의 명령어는 이런 추출에 쓸 수 있는 도구 예시입니다. 이번 작업에서는 브라우저 맵과 Node 맵, 타입 선언을 함께 다루는 스크립트를 썼습니다.

중요한 조건은 원본 내용이 들어 있어야 한다는 겁니다. sourcesContent가 없거나 null이면 원본을 따로 확보해야 합니다. 위치를 연결하는 mappings만으로는 코드 전문을 되살릴 수 없습니다.
-->

---

<!-- _header: 02 / RECONSTRUCTION -->

## 기능을 꺼도, 참조는 남았습니다

원본 SDK는 플래그에 따라 선택 기능을 등록했습니다

```ts
// 원본 조립 구조의 요약
static create({ optional }: { optional: boolean }) {
  if (optional) registry.register(new OptionalModule(...))
  // OptionalModule을 향하는 참조는 남아 있다
}
```

이 빌드에서는 플래그가 항상 `false`라고 접히지 않았습니다

> **기능 비활성화 ≠ 코드 참조 제거**

<!--
발표 50초 / 09:40~10:30

복원한 소스에서 먼저 초기화 경로를 따라갔습니다. 원본에도 선택 기능을 끄는 플래그가 있었습니다. optional이 참일 때만 기능을 등록하는 구조입니다.

하지만 이번 빌드에서는 이 값이 항상 거짓이라고 확정되지 않았습니다. 그래서 OptionalModule을 참조하는 코드가 남았습니다. 실행할 때 기능을 꺼 두는 것만으로는 번들에서 그 코드가 빠지지 않았던 겁니다.

이제 필요한 건 플래그 값을 바꾸는 것이 아니라, 전송 경로가 선택 기능을 참조하지 않는 구조였습니다.
-->

---

<!-- _class: assembly compact -->

<!-- _header: 02 / RECONSTRUCTION -->

## 선택 기능으로 가는 import를 끊었습니다

```ts
// BEFORE: 코어가 선택 기능을 직접 조립
import {OptionalModule} from './optional/OptionalModule'
static create({optional}) {
  if (optional) registry.register(new OptionalModule())
}

// AFTER: 바깥의 별도 파일이 조립하고, 코어는 완성품만 받음
static create(assembled: Assembled) { /* 조립된 것을 받기만 */ }
```

> 코어 → 선택 기능의 import **0개**<br>그제야 전송용 번들에서 관련 코드가 빠졌습니다

_선택 기능이 필요한 엔트리에서만 조립 파일을 가져갑니다. 코드와 경로는 구조를 요약한 가명입니다_

<!--
발표 65초 / 10:30~11:35

그래서 선택 기능을 만들고 등록하는 코드, 즉 조립하는 위치를 바꿨습니다.

위쪽 원본에서는 코어가 선택 기능을 직접 import하고, 조건을 보고 조립합니다. 이 구조에서는 앞에서 본 것처럼 플래그를 꺼도 선택 기능의 참조가 남았습니다.

아래쪽에서는 조립을 코어 바깥의 별도 파일로 옮겼습니다. 코어는 완성된 것을 받기만 합니다. 선택 기능이 필요한 엔트리에서만 그 조립 파일을 가져오고, 전송용 엔트리에서는 가져오지 않습니다.

이렇게 코어에서 선택 기능으로 가는 import를 없애자, 전송용 번들에서 관련 코드가 빠졌습니다. 핵심 로직을 다시 쓰지 않고, 어떤 엔트리가 어떤 기능을 가져오는지를 바꾼 겁니다.
-->

---

<!-- _class: compact -->

<!-- _header: 02 / RECONSTRUCTION -->

## 의존성의 크기보다, 사용처와 계약부터

| 대상                 | 변경과 확인                                      |
| -------------------- | ------------------------------------------------ |
| **폴리필**           | 사용처 조사 후 제거, `flat`과 `flatMap` 3곳 치환 |
| **UUID**             | 플랫폼 API로 교체, 기존 UUID 형식 유지           |
| **base64**           | 플랫폼 API로 교체, 원본 라이브러리와 대조        |
| **쿼리 문자열 처리** | 표준 API로 교체, 반복 키와 공백 인코딩 규칙 대조 |

외부 런타임 의존성 **0개** / 빌드 문법 타깃 **ES2017**

`TextEncoder`, `btoa`, Web Crypto는 실행 환경이 제공해야 합니다<br>**각 API의 지원 하한을 팀 browserslist 타깃과 대조했습니다**

<!--
발표 80초 / 11:35~12:55

조립을 나눈 뒤에는 SDK에 남아 있는 의존성을 살폈습니다. 여기서도 크기보다 사용처와 출력 계약을 먼저 봤습니다. 폴리필은 실제로 쓰는 곳을 조사한 뒤 제거했고, flat과 flatMap을 쓰던 세 곳은 치환했습니다. UUID는 기존 형식을 유지하고, base64는 원본 라이브러리와 결과를 대조했습니다.

쿼리 문자열처럼 익숙한 처리도 같은 기준으로 봐야 합니다. 표준 API로 바꿀 수 있어도, 반복되는 키나 공백을 처리하는 방식이 다르면 결과가 달라질 수 있습니다. 짧게 구현하는 것보다 기존 출력 규칙을 지키는 게 먼저입니다.

이렇게 외부 런타임 의존성을 없애고 문법 타깃을 ES2017로 맞췄습니다. 다만 문법 타깃과 API 지원은 별개입니다. TextEncoder, btoa, Web Crypto의 지원 하한도 저희 팀 browserslist 타깃과 대조했습니다.
-->

---

<!-- _class: results -->

<!-- _header: 02 / RECONSTRUCTION -->

## 필요한 기능만 가져가도록 바꾼 결과

> **−69%**
>
> 전송용 번들 gzip / 94.9 → 29.5 KiB

| 가져가는 엔트리             | raw           | gzip         | 벤더 대비 gzip 감소율 |
| --------------------------- | ------------- | ------------ | --------------------- |
| **`./send` 전송용**         | **100.0 KiB** | **29.5 KiB** | **약 −69%**           |
| `./extended` 선택 기능 포함 | 173.2 KiB     | 46.5 KiB     | 약 −51%               |
| `.` 전체 엔트리             | 264.3 KiB     | 66.0 KiB     | 약 −30%               |
| 벤더 원본                   | 383.4 KiB     | 94.9 KiB     | 기준                  |

**엔트리 분리 + 빌드 + 폴리필 + 의존성 변경을 함께 반영한 결과입니다**

_2026-09-05 갱신본 / 패키지 이름으로 import / esbuild 0.24.2 / browser/esm/es2017 / gzip 레벨 9<br>초기 진단과는 별도 측정. raw는 minify 후 압축 전 크기 (1 KiB = 1,024 B)_

<!--
발표 60초 / 12:55~13:55

여기까지 적용한 결과입니다. 앞의 74%는 상수만 가져오는 예제에 PURE 주석을 넣은 실험이었고, 이 표는 SDK를 재구성한 뒤의 결과입니다. 갱신한 버전을 패키지로 가져와 별도로 측정했습니다.

맨 아래가 벤더 원본이고, 맨 위가 저희가 쓰는 전송용입니다. 전송용은 gzip 기준 약 69% 줄었습니다. 선택 기능을 포함한 엔트리는 약 51%, 전체 엔트리도 약 30% 줄었습니다.

엔트리 분리만의 효과는 아닙니다. 빌드 설정, 폴리필, 의존성 변경을 함께 반영한 결과입니다. 필요한 기능만 가져가도록 나누고, 각 엔트리에 공통으로 들어가던 비용도 줄였습니다.
-->

---

<!-- _class: chapter -->

<!-- _header: 03 / VERIFICATION -->

# 작아졌습니다<br>그런데, 동작도 같을까요?

## 조립, 엔트리, 의존성을 전부 바꿨습니다

> 03

<!--
발표 10초 / 13:55~14:05

크기는 줄었습니다. 하지만 조립과 엔트리, 의존성을 바꿨습니다. 그럼 남겨 둔 동작은 원본과 같을까요?
-->

---

<!-- _header: 03 / VERIFICATION -->

## 남겨 둔 동작을 엔트리별로 대조했습니다

전송용뿐 아니라, **선택 기능을 포함한 엔트리도 함께 재구성했습니다**

| 엔트리               | 원본과 대조한 대상           |
| -------------------- | ---------------------------- |
| **전송용**           | 전송 데이터의 비교 대상 필드 |
| **선택 기능과 전체** | 조건별 반환값과 후속 동작    |

> 전송용에는 **초기화 순서와 일부 부가 필드의 의도적 차이**가 있습니다

**이 차이가 수집과 집계에 미치는 영향은 별도 검증 대상입니다**

<!--
발표 50초 / 14:05~14:55

먼저 무엇이 같아야 하는지 정했습니다. 앞의 표처럼 전송용뿐 아니라 선택 기능을 포함한 엔트리도 재구성했기 때문에, 각 엔트리에 남겨 둔 동작을 확인해야 했습니다.

전송용은 전송 데이터의 비교 대상 필드를, 선택 기능과 전체 엔트리는 조건별 반환값과 후속 동작을 대조했습니다. 뒤에서 보여드릴 조건 판정의 실패 사례는 이쪽을 검증하다 발견한 것입니다.

전송용에는 초기화 순서와 일부 부가 필드의 의도적인 차이도 있습니다. 그 차이는 따로 확인하고, 수집과 집계에 미치는 영향은 별도 검증 대상으로 구분했습니다.
-->

---

<!-- _class: compact -->

<!-- _header: 03 / VERIFICATION -->

## 같은 입력을, 원본과 재구성본에 넣었습니다

```ts
// 대조 구조를 요약한 의사 코드
const original = await bootOriginal(isolatedWindow, config)
const rebuilt = await bootRebuilt(config)

expect(await runScenario(rebuilt, input)).toEqual(
  await runScenario(original, input),
)
```

**같은 입력과 같은 설정 → 반환값과 후속 동작 대조**

전송 데이터는 비교할 필드를 정했습니다. 난수, 시각, 의도적 차이는 따로 다뤘습니다

_소스뿐 아니라 빌드된 패키지의 각 엔트리도 대표 시나리오로 대조합니다_

<!--
발표 60초 / 14:55~15:55

비교할 대상을 정한 다음에는, 무엇을 기준으로 맞다고 판단할지가 문제였습니다. 기대값을 제가 직접 정하는 테스트만으로는 부족하다고 봤습니다. 원본을 잘못 이해했다면, 그 오해를 그대로 기대값에 넣을 수도 있으니까요.

그래서 원본을 대조군으로 썼습니다. 원본 SDK와 재구성한 SDK를 각각 띄우고, 같은 설정과 입력으로 같은 시나리오를 실행했습니다. 반환값과 후속 동작, 전송 데이터에서 비교하기로 정한 필드를 맞춰 봤습니다.

난수와 시각처럼 매번 달라지는 값은 따로 다뤘고, 의도적으로 바꾼 부가 필드는 양쪽 값을 각각 확인했습니다. 소스뿐 아니라 빌드한 패키지의 각 엔트리도 대표 시나리오로 대조했습니다.
-->

---

<!-- _class: verdict -->

<!-- _header: 03 / VERIFICATION -->

# 당시 테스트는 모두 통과했습니다

> **0건**

## 대조 대상으로 삼은 값의 불일치

동작이 달라지면, **이 테스트는 실패할까요?**

<!--
발표 20초 / 15:55~16:15

대조 결과는 불일치 0건이었고, 당시 테스트는 모두 통과했습니다.

그런데 이 테스트가 실제로 차이를 잡아내는지도 확인하고 싶었습니다. 동작이 달라지도록 코드를 바꾸면, 이번에는 실패할까요?
-->

---

<!-- _class: mutation compact -->

<!-- _header: 03 / VERIFICATION -->

## 이번에는, 테스트가 실패해야 정상입니다

> `>` → `>=`

**소스를 일부러 바꾸고, 어떤 테스트가 실패하는지 확인합니다**
뮤테이션 테스트입니다

```js
{
  find:    'value > threshold',
  replace: 'value >= threshold',
  detects: '경계값 대조 테스트' // 이 테스트가 실패해야 정상
}
```

_미검출: 코드 미도달, 부족한 경계 입력과 단언, 결과가 같은 변경 등을 구분해 조사해야 합니다_

<!--
발표 55초 / 16:15~17:10

그걸 확인하려고 SDK 소스를 일부러 바꾸고, 어떤 테스트가 실패하는지 봤습니다. 뮤테이션 테스트입니다.

예를 들어 크다를 크거나 같다로 바꾸면, 기준값과 정확히 같을 때 결과가 달라집니다. 그 경계를 확인하는 테스트라면 변경된 코드에서 실패해야 합니다.

아무 테스트도 실패하지 않았다면 이유를 찾아야 합니다. 해당 코드에 도달하지 않았을 수도 있고, 경계 입력이나 결과를 확인하는 단언이 빠졌을 수도 있습니다. 동작상 차이가 없는 변경인지도 구분해야 합니다. 안 걸렸다는 결과만으로 원인을 알 수는 없습니다.
-->

---

<!-- _header: 03 / VERIFICATION -->

## 한 곳을 바꾸고, 전체를 돌리고, 되돌립니다

**고른 변경 약 100개 × 매번 약 2천 개의 테스트**

| 결과          | 어떻게 해석했나                                |
| ------------- | ---------------------------------------------- |
| **검출**      | 실제 단언 실패, 실패한 테스트 이름까지 확인    |
| **미검출**    | 현재 입력과 단언으로 변경을 구별하지 못함      |
| **실행 오류** | 실행, 수집, 리포트 오류 / 검출로 집계하지 않음 |

> 처음에는 **안 걸리는 변경**이 있었습니다. 그 원인을 추적했습니다

_직접 선택한 변경 지점에 대한 검증입니다_

<!--
발표 60초 / 17:10~18:10

먼저 원래 코드에서 테스트가 통과하는지 확인합니다. 그다음 소스 한 곳을 바꾸고, 테스트를 돌린 뒤, 코드를 원래대로 되돌립니다. 마지막에는 제가 고른 변경 약 100개를, 변경마다 약 2천 개의 테스트로 확인했습니다.

결과도 구분했습니다. 실제 단언이 실패하고 실패한 테스트까지 확인되면 검출입니다. 테스트가 통과하면 미검출입니다. 테스트 실행이나 결과 수집에서 난 오류는 별도로 뺐습니다. 실행이 멈췄다는 것만으로 변경을 잡았다고 셀 수는 없으니까요.

처음부터 모두 걸렸던 건 아닙니다. 지금부터는 그 과정에서 발견한 세 가지 빈틈을 보여드리겠습니다.
-->

---

<!-- _class: compact -->

<!-- _header: 03 / VERIFICATION / CASE 01 -->

## 판정을 무조건 true로 바꿨는데, 통과했습니다

“조건이 안 맞으면 실행하지 않는다”는 테스트가 실패해야 했습니다

```ts
// 공유 함수를 감싸는 구조의 요약
const original = shared.handler
shared.handler = function (...args) {
  original.apply(this, args)
  instance.handleChange()
}
// 인스턴스를 종료해도, 이 래퍼는 남아 있었다
```

> **앞 테스트의 인스턴스가 종료 뒤에도 남아** 상태 변화에 계속 반응했습니다

<!--
발표 65초 / 18:10~19:15

첫 번째 사례입니다. SDK 안에 조건이 맞으면 후속 동작을 실행하는 경로가 있었습니다. 여기서 판정을 아예 무조건 참으로 바꿨습니다. 조건을 안 보고 무조건 실행하게 만든 겁니다.

그러면 조건이 안 맞는 경우에도 실행될 테니, 조건이 안 맞으면 실행하지 않는다를 확인하는 테스트는 당연히 실패해야 합니다.

그런데 통과했습니다.

원인을 따라가 보니 이 구조였습니다. SDK가 공유 함수를 감싸서 자기 인스턴스의 처리를 끼워 넣습니다. 문제는 인스턴스를 종료해도 이 래퍼가 공유 함수에 그대로 남아 있었다는 겁니다. 그래서 앞 테스트에서 만든 인스턴스가, 종료된 뒤에도 상태 변화에 계속 반응하고 있었습니다.
-->

---

<!-- _class: flow -->

<!-- _header: 03 / VERIFICATION / CASE 01 -->

## 망가진 판정문에 도달조차 하지 않았습니다

1. **이전 인스턴스**<br>무조건 true<br>→ 후속 동작 실행<br>→ 중복 처리 방지 표시
2. **이번 인스턴스**<br>이미 처리된 표시 확인<br>→ 조기 종료<br>→ 판정문 실행 안 함
3. **테스트 통과**<br>“이번 인스턴스의<br>후속 동작 없음”<br>→ 기대와 일치

> 망가진 코드를 **실행하지 않아서** 통과했습니다

테스트 부팅 전에 **공유 함수를 원래 구현으로 복원**했습니다. 같은 고의 변경을 다시 넣자, 테스트가 실패했습니다

_SDK의 종료 동작이 아니라 테스트 격리를 고쳤습니다_

<!--
발표 95초 / 19:15~20:50

이전 인스턴스가 남았는데, 왜 이번 테스트는 통과했는지 보겠습니다.

먼저 상태가 바뀝니다. 그러면 살아남아 있던 이전 인스턴스가 먼저 반응합니다. 이 인스턴스도 판정이 무조건 참으로 바뀌어 있으니까, 조건을 안 보고 후속 동작을 실행합니다. 그리고 중복 처리를 막으려고 이미 처리했다는 표시를 남깁니다.

그다음에 이번 테스트가 만든 인스턴스가 반응할 차례입니다. 그런데 이미 표시가 있으니까 조기 종료합니다. 제가 망가뜨려 놓은 판정문까지 가지도 못하는 겁니다.

테스트 입장에서는 어떻게 보이냐면, 이번 인스턴스가 후속 동작을 하지 않았습니다. 기대한 대로죠. 그래서 통과합니다. 망가진 코드가 옳아서가 아니라, 망가진 코드를 실행조차 하지 않아서 통과한 겁니다.

그래서 테스트를 띄우기 전에 공유 함수를 원래 구현으로 복원하도록 고쳤습니다. 그리고 같은 고의 변경을 다시 넣었더니, 이번에는 테스트가 제대로 실패했습니다.

여기서 제가 고친 것은 테스트 격리라는 점을 짚고 싶습니다. SDK의 종료 동작 자체는 원본과 맞춰야 하니까 그대로 뒀습니다.
-->

---

<!-- _header: 03 / VERIFICATION / CASE 02 -->

## 입력은 많았지만, 같은 경로만 밟았습니다

`>` → `>=`, “포함” → “시작”으로 바꿔도 **모두 통과했습니다**

설정된 조건이 **전부 목록 포함 여부만 검사**하고 있었기 때문입니다

| 빠진 경로               | 추가한 입력                       |
| ----------------------- | --------------------------------- |
| `>`와 `>=`의 차이       | 기준값과 **정확히 같은 값**       |
| 포함과 시작 매칭의 차이 | 문자열이 **중간에만 나타나는 값** |

> 연산자별 설정으로 **대조 경로를 넓히고,** 같은 변경을 다시 넣어 검출하는지 확인했습니다

<!--
발표 70초 / 20:50~22:00

두 번째는 테스트 입력의 문제였습니다. 숫자 비교는 “크다”를 “크거나 같다”로 바꿨습니다. 문자열 판정은 “포함한다”를 “그 문자열로 시작한다”로 바꿨습니다. 그런데 아무 테스트도 실패하지 않았습니다.

이유를 보니, 테스트 설정에 넣은 조건이 전부 목록에 포함되는지만 검사하고 있었습니다. 입력은 여러 개를 대조했는데, 실은 같은 경로만 반복해서 밟고 있었던 겁니다. 부등호를 쓰는 경로에는 애초에 들어가지도 않았습니다.

그래서 기준값과 정확히 같은 값, 문자열이 중간에만 나타나는 값처럼 빠진 경로를 겨냥한 입력을 추가했습니다. 연산자별 설정을 넣어서 대조 경로를 넓힌 겁니다. 그리고 같은 고의 변경을 다시 넣어서, 이번에는 걸리는지 확인했습니다.
-->

---

<!-- _class: flow -->

<!-- _header: 03 / VERIFICATION / CASE 03 -->

## 양쪽이 똑같이 아무것도 안 해도, 일치합니다

연산자별 설정을 추가하다가 **이미 쓰던 키를 다시 붙였습니다**

1. **키 중복**<br>맵에서 뒤 설정이<br>앞 설정을 덮어씀
2. **검사할 설정 소실**<br>원래 검사할 입력이<br>조용히 사라짐
3. **출력 일치**<br>양쪽 모두 “설정 없음”<br>→ 대조 테스트 통과

> 같은 잘못된 입력은 **출력 대조만으로 잡을 수 없습니다**

그래서 **입력의 키가 겹치면 실패하는 검사**를 추가했습니다

<!--
발표 80초 / 22:00~23:20

세 번째 사례인데, 이건 제가 낸 사고입니다. 방금 말씀드린 대로 연산자별 설정을 추가하고 있었는데, 그러다가 이미 쓰고 있던 키를 다시 붙였습니다. 그랬더니 잘 잡히던 고장들이 갑자기 안 잡히기 시작했습니다.

무슨 일이 있었냐면, 설정은 키를 이름표로 해서 맵에 담깁니다. 키가 겹치면 뒤에 넣은 설정이 앞 설정을 덮어씁니다. 제가 원래 검사하려던 설정이 입력에서 조용히 사라진 겁니다. 이때 에러는 나지 않습니다. 맵에 같은 키로 넣는 건 정상 동작이니까요.

그다음이 중요합니다. 검사할 설정이 없으니 양쪽 SDK가 똑같이 설정 없음을 돌려줍니다. 그러면 비교 결과는 일치입니다. 테스트는 통과합니다.

여기서 배운 게 이겁니다. 양쪽이 같은 이유로 아무것도 하지 않아도, 출력은 일치합니다. 대조라는 방법 자체의 빈틈이죠. 그래서 입력의 키가 겹치면 아예 실패하도록 검사를 따로 추가했습니다.
-->

---

<!-- _header: 03 / VERIFICATION -->

## 여러분의 테스트도 한 번 망가뜨려 보세요

| 먼저 확인할 것                 | 일부러 바꾸고 확인할 것          |
| ------------------------------ | -------------------------------- |
| 종료 후 전역 상태가 격리됐는가 | 실행 순서를 바꿔도 결과가 같은가 |
| 준비 후 판정문에 도달하는가    | 필요한 설정을 빼면 실패하는가    |
| 경계값과 비매치 입력이 있는가  | `>`를 `>=`로 바꾸면 실패하는가   |
| 실제 전송을 확인하는가         | 전송 함수를 비우면 실패하는가    |

> **출력 일치뿐 아니라, 기대한 상태와 이유까지 확인하세요**

_“호출되지 않았다”만으로 올바른 분기를 탔다고 판단할 수 없습니다. 청중 적용 예시입니다_

<!--
발표 55초 / 23:20~24:15

세 사례에서 빠져 있던 것은 각각 테스트 격리, 실행할 경로, 입력 자체의 유효성이었습니다. 여러분의 테스트에서도 이렇게 확인해 볼 수 있습니다.

호출되지 않아야 한다는 테스트라면, 판정을 무조건 참으로 바꿔 보세요. 그래도 통과한다면 그 판정문까지 갔는지, 앞 테스트의 상태가 남아 있지는 않은지 확인해야 합니다.

부등호를 바꿔도 통과한다면 경계 입력을, 전송 함수를 비워도 통과한다면 전송을 확인하는 단언을 살펴볼 수 있습니다.

핵심은 출력이 일치한 이유까지 확인하는 겁니다. 입력이 유효한지, 검사하려던 코드에 도달했는지, 앞 테스트의 상태가 섞이지 않았는지를 함께 봐야 합니다.
-->

---

<!-- _class: limits -->

<!-- _header: FECONF 2026 / SCOPE & RESPONSIBILITY -->

## 확인한 범위와, 남아 있는 범위

| 확인한 것                    | 근거                                |
| ---------------------------- | ----------------------------------- |
| 이벤트의 비교 대상 필드      | 원본 UMD와 같은 입력으로 대조       |
| 전송 엔트리의 부가 필드 차이 | 차이가 있는 양쪽 값을 각각 단언     |
| 엔트리별 대표 동작           | 빌드한 패키지를 격리 jsdom에서 대조 |

> **별도 검증이 필요합니다**<br>실제 웹뷰의 전송과 이탈 처리 → 서버 수집 → 대시보드 집계

부가 필드 차이를 서비스에서 허용할지는 **그 결과로 판단해야 합니다**

<!--
발표 55초 / 24:15~25:10

이렇게 테스트의 빈틈을 보완했습니다. 그러면 이 검증으로 어디까지 확인했다고 말할 수 있을까요?

원본과 대조한 것은 이벤트의 비교 대상 필드와 엔트리별 대표 동작입니다. 의도적으로 달라진 부가 필드는 양쪽 값을 각각 확인했습니다. 이 대조는 격리한 jsdom 환경에서 했습니다.

이 결과만으로 실제 웹뷰의 전송과 이탈 처리, 서버 수집, 대시보드 집계까지 같다고 말할 수는 없습니다. 그 경로는 별도로 검증해야 합니다. 부가 필드의 차이를 서비스에서 허용할지도, 수집과 집계에 미치는 영향을 보고 판단해야 합니다.
-->

---

<!-- _class: takeaways compact -->

<!-- _header: FECONF 2026 / SCOPE & RESPONSIBILITY -->

## 작아진 SDK는, 이제 직접 유지해야 합니다

1. **버전이 바뀌면 다시 대조합니다**<br>로직, 전송 계약, 검증 입력 확인<br>원본 배포물은 소스와 별도 준비
2. **소스 밖의 계약도 확인합니다**<br>뮤테이션 테스트, 빌드 산출물, 타입 소비, 번들 크기
3. **복귀할 방법까지 비용에 넣습니다**<br>재추출은 수정 소스를 덮어씁니다. 갱신 작업을 끝내 주지는 않습니다

_확보한 근거는 재구성과 코드, 번들 검증까지입니다. 갱신 비용과 운영 검증도 교체 비용에 포함합니다_

<!--
발표 50초 / 25:10~26:00

검증은 다음 버전에서도 이어집니다. 재구성한 SDK는 이후의 유지 비용도 저희가 감당해야 합니다.

벤더 버전이 바뀌면 변경된 로직과 전송 계약을 확인하고, 원본 대조와 테스트를 다시 돌려야 합니다. 소스뿐 아니라 빌드한 패키지, 소비하는 쪽의 타입, 번들 크기도 확인 대상입니다.

소스맵에서 다시 추출하면 수정해 둔 파일을 덮어쓸 수 있다는 점도 주의해야 합니다. 원본 배포물을 별도로 준비해 두고, 바뀐 부분을 비교해서 반영해야 합니다.

크기가 줄어든 이득과 함께, 이런 갱신 작업과 운영 검증, 원본으로 되돌아갈 방법까지 교체 비용에 넣어야 합니다.
-->

---

<!-- _class: closing -->

<!-- _header: FECONF 2026 / TAKEAWAYS -->

# 테스트도 검증 대상입니다

## 전송용 번들 gzip, 약 69% 감소

필요 없는 기능으로 이어지는 **import 경로를 끊었습니다**

원본과 같은 입력으로 대조하고, 고의 변경으로 **테스트가 놓친 경로를 찾았습니다**

> 동작 보존의 근거를 얻으려면, **테스트 자체도 검증해야 했습니다**

<!--
발표 35초 / 26:00~26:35

이번 작업에서는 전송용 번들의 gzip 크기를 약 69% 줄였습니다. 필요 없는 기능으로 가는 import를 끊고, 빌드와 의존성을 정리한 결과입니다.

그 과정에서 더 중요했던 것은 검증이었습니다. 원본과 대조해서 모두 통과한 테스트도, 고의로 코드를 바꾸자 놓치고 있던 경로가 드러났습니다. 동작을 보존했다는 근거를 얻으려면 테스트 자체도 검증해야 했습니다.
-->

---

<!-- _class: takeaways -->

<!-- _header: FECONF 2026 / TAKEAWAYS -->

## 크기, 동작, 유지 비용까지가 최적화입니다

1. **병목은 측정하고, 구조까지 바꿔 봅니다**<br>원인을 가른 뒤 빌드와 의존성, 엔트리 재구성을 검토합니다
2. **소스맵이 없어도 선택지는 있습니다**<br>공개 저장소, 패키지 소스, 비압축 배포본을 확인합니다<br>확보가 어렵다면 지연 로딩, 사용 범위 축소, 대체 SDK를 비교합니다
3. **동작 검증과 유지 비용을 함께 판단합니다**<br>원본 대조, 테스트 검증, 버전 갱신과 운영 검증까지 포함합니다

<!--
발표 45초 / 26:35~27:20

같은 SDK를 쓰지 않더라도, 다른 외부 의존성을 다룰 때 적용할 수 있는 점은 세 가지입니다.

첫째, 측정으로 병목을 확인하고, 힌트나 설정만으로 해결되지 않으면 빌드와 엔트리, 의존성 구조까지 살펴보는 겁니다.

둘째, 소스맵이 없어도 공개 저장소나 패키지 소스, 비압축 배포본을 확인해 볼 수 있습니다. 원본을 확보하기 어렵다면 지연 로딩이나 사용 범위 축소, 대체 SDK도 선택지입니다.

셋째, 크기 감소와 동작 검증, 이후의 유지 비용을 함께 판단해야 합니다.
-->

---

<!-- _class: resources outcomes -->

<!-- _header: FECONF 2026 / HOME IMPACT -->

# 증권홈에서 이렇게 개선했습니다

SDK 단독 효과가 아닌, 번들 정리와 렌더링 개선을 합친 홈 전체 성과

| 문서 load  | FCP        | DCL        | 내려받는 JS | 초기 5초 CPU |
| ---------- | ---------- | ---------- | ----------- | ------------ |
| **−47.5%** | **−44.3%** | **−41.6%** | **−38.2%**  | **−30.0%**   |

## 홈에서 한 그 밖의 최적화/개선 사례

- [외부 SDK를 뜯어서 다시 만들기](https://yceffort.kr/2026/08/rebuilding-a-vendor-sdk)<br>이번 발표의 상세 기록: 소스 복원, 번들 재구성, 검증
- [framer-motion 프레임드랍 없애기](https://yceffort.kr/2026/08/framer-motion-banner-frame-drop)<br>원본 모션을 대조하며 레이아웃 비용 줄이기
- [number-flow를 구형 브라우저로 이식하기](https://yceffort.kr/2026/08/number-flow-fork-for-old-browsers)<br>API와 시각 결과를 보존하며 애니메이션 구동부 교체
- [실행하지 않는 JavaScript를 10MiB까지 늘려봤다](https://yceffort.kr/2026/09/unused-javascript-cost)<br>크기와 파싱, 컴파일, 초기화 비용을 분리한 측정 기록

<!--
발표 45초 / 27:20~28:05

홈에서는 이 SDK 작업 외에도 번들 정리와 렌더링 개선을 진행했습니다. 이 표는 그 작업들을 함께 적용한 홈 전체의 결과입니다. SDK 하나를 바꿔서 얻은 효과와는 구분해서 봐야 합니다.

같은 조건에서 개선 전후를 비교했을 때, 문서 로드는 47.5%, FCP는 44.3%, DCL은 41.6%, 내려받는 JavaScript는 38.2%, 초기 5초 CPU 사용은 30% 줄었습니다. 관련 작업과 실험은 아래 네 편에 정리했습니다.
-->

---

<!-- _class: closing thanks -->

<!-- _header: FECONF 2026 / THANK YOU -->

# 감사합니다

카카오페이증권 / 김용찬

[yceffort.kr](https://yceffort.kr) / [github.com/yceffort](https://github.com/yceffort)

<!--
발표 15초 / 28:05~28:20

지금까지 카카오페이증권 김용찬이었습니다. 들어주셔서 감사합니다.
-->

---

<!-- _header: APPENDIX / 01 -->

<!-- _class: hidden-slide -->

## 백업: Next.js 공유 청크의 측정 조건

Next.js 16 + Turbopack의 **한 빌드 안에** 라우트 셋을 두었습니다
이 실험은 SDK를 재export하는 **앱 연동용 래퍼 패키지**를 import합니다

```bash
$ grep -o 'static/chunks/[a-z0-9_-]*\.js' .next/server/app/min.html | sort -u
static/chunks/0k_6yl401mp01.js   # 페이지 고유
static/chunks/2izqmkhuyaa9a.js   # min과 init이 공유하는 SDK 청크
```

| 라우트      | 하는 일            | SDK 청크 raw |
| ----------- | ------------------ | ------------ |
| `/baseline` | SDK 없음           | 없음         |
| `/min`      | 상수 하나 import   | 약 400 KiB   |
| `/init`     | 초기화 + 전송 호출 | 같은 청크    |

**상수만 쓰는 페이지도 이 SDK 청크를 로드했습니다**
제거 가능성은 같은 진입점의 최소 import와 초기화를 **별도 빌드**해서 비교해야 합니다

---

<!-- _header: APPENDIX / 02 -->

<!-- _class: hidden-slide -->

## 백업: 폴리필 사용처와 실행 환경

| 조사 대상             | 결과                                        |
| --------------------- | ------------------------------------------- |
| Promise, async/await  | 90회 이상, API 지원과 문법 타깃을 각각 확인 |
| `flat` / `flatMap`    | 3곳을 `reduce`로 치환                       |
| 추가 주입 메서드 10종 | 조사한 사용처 0회                           |

폴리필 import를 제거하고 **빌드 문법 타깃을 ES2017**로 맞췄습니다
다만 `TextEncoder`, `btoa`, Web Crypto는 실행 환경이 제공해야 합니다

**ES2017로 빌드했다는 사실만으로 브라우저 호환성이 보장되지는 않습니다**
그래서 각 API의 지원 하한을 문서에서 확인하고 팀 browserslist 타깃과 대조했습니다

---

<!-- _header: APPENDIX / 03 -->

<!-- _class: hidden-slide -->

## 백업: base64 구현과 원본 대조

```ts
// 우리 구현. 플랫폼 내장 TextEncoder와 btoa만 쓴다
export function encodeBase64Url(input: string): string {
  const bytes = new TextEncoder().encode(input) // btoa는 latin1 밖이면 예외
  let bin = ''
  for (const b of bytes) bin += String.fromCharCode(b)
  return btoa(bin).replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/, '')
}
```

이 결과는 **외부로 전송되는 값**입니다. 인코딩 결과가 원본과 같아야 합니다

```ts
import {Base64} from 'base64-lib' // 걷어낸 원본 라이브러리(가명). devDependency로만 남긴다
for (let n = 0; n <= 64; n++) {
  expect(encodeBase64Url('x'.repeat(n))) // 우리 구현
    .toBe(Base64.encodeUrl('x'.repeat(n))) // 원본 라이브러리
}
// 0~64는 패딩 경계(길이 % 3) 전수. 시드 고정 난수 유니코드 2000건도 같은 방식으로 대조
```

---

<!-- _header: APPENDIX / 04 -->

<!-- _class: hidden-slide -->

## 백업: grep 예측과 번들 그래프

원본 base64 라이브러리를 걷어내면서 `grep` 결과를 보고 **"`./send` 엔트리에는 영향 없을 것이다"** 라고 예측했습니다
그런데 번들 그래프에는 **전송 엔트리에서 base64로 이어지는 경로**가 있었습니다

```bash
$ grep -rl base64 src                    # 파일 목록으로 본 것
src/util/base64.ts
src/fetcher/MetadataLoader.ts
src/fetcher/ConfigLoader.ts  # 엔트리 파일이 없으니 send와 무관하다고 봤다
```

```bash
$ esbuild src/index.send.ts --bundle --metafile=meta.json   # 번들 그래프로 본 것
index.send.ts → … → ConfigLoader.ts → base64.ts
# metafile의 inputs/imports를 따라가 확인한 경로 (중간 모듈 생략)
```

지금은 번들 산출물의 metafile에서 **엔트리까지 이어지는 import 경로**를 확인합니다

---

<!-- _header: APPENDIX / 05 -->

<!-- _class: hidden-slide -->

## 백업: 홈 전체 최적화의 관측 결과

번들 정리와 렌더링 개선을 함께 적용한 홈 전체 성과입니다
SDK 단독 기여분은 분리 측정하지 않았습니다

| 지표         | 감소율 |
| ------------ | ------ |
| 문서 load    | 47.5%  |
| FCP          | 44.3%  |
| DCL          | 41.6%  |
| 내려받는 JS  | 38.2%  |
| 초기 5초 CPU | 30.0%  |

모바일 에뮬레이션, CPU 4배 감속, 콜드 로드
같은 조건에서 비교한 before 4회와 after 5회의 중앙값입니다

---

<!-- _header: APPENDIX / 06 -->

<!-- _class: hidden-slide -->

## 백업: UUID 형식을 유지하며 바꿨습니다

```ts
export function v4(): string {
  const c = globalThis.crypto
  if (typeof c.randomUUID === 'function') return c.randomUUID()

  // 없으면 직접 조립한다: 랜덤 16바이트를 받아서
  const b = new Uint8Array(16)
  c.getRandomValues(b)
  b[6] = (b[6] & 0x0f) | 0x40 // 7번째 바이트 상위 4비트 = 버전(4)
  b[8] = (b[8] & 0x3f) | 0x80 // 9번째 바이트 상위 2비트 = 변형(RFC 4122)
  /* ...hex 문자열로 조립... */
}
```

**왜 폴백이 필요한가?** `randomUUID` 에는 `[SecureContext]` 제약이 있습니다
보안 컨텍스트가 아닌 환경에서는 사용할 수 없어, `getRandomValues` 기반 폴백을 뒀습니다

---

<!-- _header: APPENDIX / 07 -->

<!-- _class: hidden-slide -->

## 백업: 실행 환경과 출력 계약

**지원 버전뿐 아니라 실행 환경의 제약까지 확인해야 합니다**
MDN 본문과 스펙에 secure context 제약이 명시되어 있습니다
사용 환경에서 `crypto.getRandomValues`도 제공되는지 확인해야 합니다

<br>

**라이브러리 크기와 출력 계약은 별개입니다**
더 가벼운 생성기를 쓰더라도 식별자의 문자 집합과 길이, 구조가 달라지면
기존 계약을 만족하는지 다시 확인해야 합니다

<br>

이 작업에서는 **플랫폼 내장과 UUID 형식을 유지하는 폴백**을 선택했습니다

---

<!-- _header: APPENDIX / 08 -->

<!-- _class: hidden-slide -->

## 백업: 출처와 측정 범위

- **트리셰이킹과 PURE:** [트리셰이킹](https://esbuild.github.io/api/#tree-shaking), [PURE 주석](https://esbuild.github.io/api/#pure), esbuild 공식 문서
- **결과 분류와 동등한 변경:** [Stryker 문서](https://stryker-mutator.io/docs/mutation-testing-elements/mutant-states-and-metrics/), [동등한 변경 예시](https://stryker-mutator.io/docs/stryker-js/disable-mutants/)
- **UUID 실행 환경:** [MDN randomUUID](https://developer.mozilla.org/en-US/docs/Web/API/Crypto/randomUUID), [getRandomValues](https://developer.mozilla.org/en-US/docs/Web/API/Crypto/getRandomValues)

크기는 초기 진단과 갱신본의 패키지 소비 측정을 구분했습니다
화면 수치는 대략값이며, 테스트는 준비한 입력과 실행 환경의 결과입니다

---

<!-- _header: APPENDIX / 09 -->

<!-- _class: hidden-slide -->

## 백업: 소스맵이 없다면 어떻게 할까

**확보할 수 있는 코드와 유지 비용에 맞춰 최적화 방법을 고릅니다**

| 상황                       | 검토할 방법                                                                 |
| -------------------------- | --------------------------------------------------------------------------- |
| 원본 소스를 확보할 수 있다 | 공개 저장소와 패키지에 포함된 소스의 버전을 맞추고 재빌드, 엔트리 분리 검토 |
| 배포된 JavaScript만 있다   | 비압축 배포본이나 포맷팅한 번들의 호출 경로를 분석하고 제한적인 패치 검토   |
| 내부를 바꾸기 어렵다       | 벤더에 경량 엔트리 요청, 지연 로딩과 사용 화면 제한, 대체 SDK 비교          |

포맷팅은 원본 TypeScript와 이름, 파일 구조의 복원을 보장하지 않습니다
지연 로딩은 로딩 시점을 바꾸므로, 초기 수집과 필수 동작이 빠지지 않는지 확인합니다

[배포 코드와 소스맵(Chrome DevTools)](https://developer.chrome.com/docs/devtools/javascript/source-maps) / [코드 분할과 지연 로딩(web.dev)](https://web.dev/articles/optimizing-content-efficiency-javascript-startup-optimization)
